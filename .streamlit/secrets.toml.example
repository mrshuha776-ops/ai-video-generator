# Streamlit Cloud Secrets template
# Copy to .streamlit/secrets.toml (NEVER commit this file — it's gitignored)
# Dashboard -> ⋮ menu -> Settings -> Secrets (on Streamlit Cloud)
#
# Get a free Groq key at https://console.groq.com -> API Keys
# Free tier: ~14,400 requests/day, no region restrictions, works in Uzbekistan.

GROQ_API_KEY = "gsk_replace_me"